package pageclient

/*
pageclient is used for interacting with Rapid7's OpenData
Sonar dataset via webscraping instead of the API.

Not as nice or stable as the API, but not bad if you
don't have an API key ;)
*/
